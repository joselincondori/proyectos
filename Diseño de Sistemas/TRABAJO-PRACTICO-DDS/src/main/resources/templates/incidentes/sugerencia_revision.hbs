{{#partial "contenido"}} 
<h1 class="d-flex justify-content-center">Sugerencia de Revision de Incidente</h1>

<div class="container text-center">
    <p>Hola! Detectamos que te encuentras cerca de el/la <b>{{establecimiento.getNombreTipoEstablecimiento}} {{establecimiento.nombre}}</b> de el/la <b>{{entidad.getNombreTipoEntidad}} {{entidad.nombre}}</b>.<br> Nos podrias indicar si se resolvio el incidente del servicio <b>{{servicio.nombre}}</b>? <br> Muchas gracias! </p>
</div>

<div class="container-fluid d-flex justify-content-center h-50">
    <div id="map" class="h-100 w-50"></div>
    <div id="popup" class="ol-popup">
        <a href="#" id="popup-closer" class="ol-popup-closer"></a>
        <div id="popup-content"></div>
    </div>
</div>

<div class="container-fluid d-flex justify-content-around">
    <div>
        <form action="/app/incidentes/sugerencias/resolver" method="post">
            <input type="text" name="sugerenciaId" value="{{sugerenciaId}}" class="d-none">
            <input type="text" name="boton" value="no" class="d-none">
            <input type="submit" value="No, todavia sigue averiado" class="btn btn-danger">
        </form>
    </div>
    <div>
        <form action="/app/incidentes/sugerencias/resolver" method="post">
            <input type="text" name="sugerenciaId" value="{{sugerenciaId}}" class="d-none">
            <input type="text" name="boton" value="omitir" class="d-none">
            <input type="submit" value="Omitir esta vez" class="btn btn-secondary">
        </form>
    </div>
    <div>
        <form action="/app/incidentes/sugerencias/resolver" method="post">
    
            <input type="text" name="sugerenciaId" value="{{sugerenciaId}}" class="d-none">
            <input type="text" name="boton" value="si" class="d-none">
            <input type="submit" value="Si, ya esta resuelto!" class="btn btn-success">
        </form>
    </div>
</div>

<button value="{{establecimiento.ubicacion.latitud}}" id="establecimiento-sugerencia-lat" class="d-none"></button>
<button value="{{establecimiento.ubicacion.longitud}}" id="establecimiento-sugerencia-lng" class="d-none"></button>
<button value="{{establecimiento.nombre}}" id="establecimiento-sugerencia-nombre" class="d-none"></button>
{{/partial}}

{{#partial "scripts"}}
    <script src="https://cdn.rawgit.com/openlayers/openlayers.github.io/master/en/v5.3.0/build/ol.js"></script>
    <script src="/js/map.js"></script>
{{/partial}}

{{> templates/base}}