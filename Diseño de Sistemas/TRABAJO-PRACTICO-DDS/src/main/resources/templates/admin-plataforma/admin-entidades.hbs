{{#partial "contenido"}}
    <h1 class="text-center">CONFIGURACION ENTIDADES</h1>

    <div style="max-width: 1000px; margin: 0 auto; padding: 50px;">
        <table class="table" style="width: 100%; max-width: 100%; table-layout: auto;">
            <thead>
            <tr class="table-primary">
                <th style="text-align: center;"> Nombre Entidad</th>
                <th style="text-align: center;"></th>
                <th style="text-align: center;"></th>
                <th style="text-align: center;"></th>
            </tr>
            </thead>

            <tbody>
            {{#each entidades}}
                <tr>
                    <td align="center">{{this.nombre}}</td>
                    <td align="center">
                        <a class="btn text-primary" href="/app/entidades/{{this.id}}/edit">Editar</a>
                    </td>
                    <td align="center">
                        <a class="btn btn-danger" href="#" data-toggle="modal" data-target="#confirmEdit{{this.id}}">Eliminar</a>

                        <div class="modal fade" id="confirmEdit{{this.id}}" tabindex="-1" role="dialog" aria-labelledby="confirmEditLabel" aria-hidden="true">
                            <div class="modal-dialog" role="document">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title" id="confirmEditLabel">Confirmación</h5>
                                        <button type="button" class="close" data-dismiss="modal" aria-label="Cerrar">
                                            <span aria-hidden="true">&times;</span>
                                        </button>
                                    </div>
                                    <div class="modal-body">
                                        ¿Seguro que quieres eliminar la entidad {{this.nombre}}?
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                                        <a class="btn btn-danger" href="/app/entidades/{{this.id}}/delete">Eliminar</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </td>
                    <td>
                        <a class="btn btn-primary" href="/app/entidades/{{this.id}}/establecimientos/edit">Editar Establecimientos</a>
                    </td>
                </tr>
            {{/each}}
            </tbody>
        </table>
    </div>

    <div class="col-md-9 offset-md-9">
        <a class="btn btn-primary" href="/app/entidades/crear">Agregar Nueva Entidad</a>
    </div>

{{/partial}}
{{> templates/base}}